const Check = () => {
  let cars = ["bmw", "mercedes", "bmw", "jagaur"];

  return (
    <div>
      <h1>IF else Cond render</h1>
      <ul>{cars.map((item, index) => {})}</ul>
    </div>
  );
};

export default Check;
